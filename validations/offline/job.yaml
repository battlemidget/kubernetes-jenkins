# Core validation test for CK

- job:
    name: 'validate-ck-offline'
    project-type: freestyle
    scm:
      - k8s-jenkins-bm
    parameters:
      - global-params
      - juju-params
    # triggers:
    #     - timed: "0 12 * * *"
    wrappers:
      - ansicolor
      - workspace-cleanup
    builders:
      - set-env
      - ogc-execute-spec
