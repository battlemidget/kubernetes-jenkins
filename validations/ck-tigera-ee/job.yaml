# Core validation test for CK w/ Calico

- job:
    name: 'ogc-validate-ck-tigera-ee'
    project-type: freestyle
    scm:
      - k8s-jenkins-bm
    parameters:
      - global-params
      - juju-params
    triggers:
        - timed: "@daily"
    wrappers:
      - ansicolor
      - workspace-cleanup
    builders:
      - set-env
      - ogc-execute-spec
