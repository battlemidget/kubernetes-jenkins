# Bugfix release

- job:
    name: 'ogc-release-bugfix'
    project-type: freestyle
    scm:
      - k8s-jenkins-bm
    parameters:
      - global-params
      - juju-params
    wrappers:
      - ansicolor
      - workspace-cleanup
    builders:
      - set-env
      - ogc-execute-spec
